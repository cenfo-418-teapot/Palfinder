<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:layout_marginHorizontal="@dimen/activity_horizontal_margin"
    tools:context=".views.auth.recover.password.RecoverPasswordActivity">

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/title_recover_password"
        style="@style/view_title"
        android:textAlignment="center"
        android:layout_margin="@dimen/activity_margin"
        android:layout_marginBottom="@dimen/activity_vertical_margin"/>

    <com.shuhart.stepview.StepView
        android:id="@+id/svRecoverySteps"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/activity_margin"
        android:layout_marginBottom="@dimen/activity_margin"
        app:sv_animationType="All"
        app:sv_doneCircleColor="@color/primaryLight"
        app:sv_doneTextColor="@color/white"
        app:sv_nextStepCircleColor="@color/secondary"
        app:sv_nextStepCircleEnabled="false"
        app:sv_nextStepLineColor="@color/primaryDark"
        app:sv_nextTextColor="@color/secondaryDark"
        app:sv_selectedCircleColor="@color/primary"
        app:sv_selectedStepNumberColor="@color/white" />

    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/vpRecoverySteps"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>